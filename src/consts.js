export const EVENTS = {
  PUSHSTATE: 'pushstate',
  POPSTATE: 'popstate' // popstate is emited when the user goes back or forward through the navigation history using the browser buttons
}

export const BUTTONS = {
  PRIMARY: 0 // The default is the left click
}
