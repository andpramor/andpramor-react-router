import{jsx as _jsx}from"react/jsx-runtime";import{useState,useEffect,Children}from"react";import{EVENTS}from"../utils/consts";import{match}from"path-to-regexp";import{getCurrentPath}from"../utils/getCurrentPath";import{Route}from"./Route";export function Router({children,routes=[],defaultComponent:DefaultComponent=()=>/*#__PURE__*/_jsx("h1",{children:"404"})}){const[currentPath,setCurrentPath]=useState(getCurrentPath());useEffect(()=>{const onLocationChange=()=>{setCurrentPath(getCurrentPath())};window.addEventListener(EVENTS.PUSHSTATE,onLocationChange);window.addEventListener(EVENTS.POPSTATE,onLocationChange);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onLocationChange);window.removeEventListener(EVENTS.POPSTATE,onLocationChange)}},[]);let routeParams={};const routesFromChildren=Children.map(children,({props,type})=>{return type===Route?props:null});const allRoutes=routesFromChildren?routesFromChildren.concat(routes).filter(Boolean):routes;const Page=allRoutes.find(({path})=>{if(path===currentPath)return true;const matcherUrl=match(path,{decode:decodeURIComponent});const matched=matcherUrl(currentPath);if(!matched)return false;routeParams=matched.params;return true})?.Component;return Page?/*#__PURE__*/_jsx(Page,{routeParams:routeParams}):/*#__PURE__*/_jsx(DefaultComponent,{routeParams:routeParams})}